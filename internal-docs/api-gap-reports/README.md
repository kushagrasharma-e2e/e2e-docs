# API Gap Reports

API gap reports record mismatches between approved OpenAPI specs and backend behavior.

Public API docs must not be silently generated from backend behavior when OpenAPI is missing or stale.
