---
interface Props {
  href?: string;
  label?: string;
}

import { docsPath } from '../utils/docsPath';

const { href = '/support-assisted/suggest-an-update/', label = 'Suggest an update' } = Astro.props;
---

<a href={docsPath(href)}>{label}</a>
